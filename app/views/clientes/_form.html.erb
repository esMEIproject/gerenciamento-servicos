<%= form_with(model: cliente) do |form| %>
  <% if cliente.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cliente.errors.count, "error") %> prohibited this cliente from being saved:</h2>

      <ul>
        <% cliente.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome, "Nome" %>
    <%= form.text_field :nome %>
  </div>

  <div class="field">
    <%= form.label :cpf, "CPF" %>
    <%= form.text_field :cpf %>
  </div>

  <div class="field">
    <%= form.label :contato, "Contato" %>
    <%= form.text_field :contato %>
  </div>

  <%= form.fields_for :endereco do |e| %>

    <div class="field">
      <%= e.label :cidade, "Cidade"%>
      <%= e.text_field :cidade %>
    </div>

    <div class="field">
      <%= e.label :bairro, "Bairro"%>
      <%= e.text_field :bairro %>
    </div>

    <div class="field">
      <%= e.label :lougradouro, "Logradouro" %>
      <%= e.text_field :logradouro %>
    </div>

    <div class="field">
      <%= e.label :complemento, "Complemento"%>
      <%= e.text_field :complemento %>
    </div>

  <% end %>

  <div class="actions">
    <%= form.submit id:"submit" %>
  </div>
<% end %>
